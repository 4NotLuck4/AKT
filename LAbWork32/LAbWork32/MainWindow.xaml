<Window x:Class="LAbWork32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LAbWork32"
        mc:Ignorable="d"
        Title="Каталог товаров" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" 
                    Margin="10,10,10,10"
                    Grid.ColumnSpan="3">
            <StackPanel Orientation="Horizontal">
                <ListBox x:Name="CategoryListBox" 
                         Width="780" Height="50"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto">

                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>


                </ListBox>
            </StackPanel>
            
        </StackPanel>

        <ListBox x:Name="AvailabilityListBox"
                 Grid.RowSpan="2">
            <TextBlock FontWeight="Bold" 
                       Text="Наличие" />
            <CheckBox Content="В наличии" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: сегодня" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: завтра" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: позже" 
                      IsChecked="True"/>
            <CheckBox Content="Нет в продаже"/>
            <Button Content="Применить"/>
        </ListBox>
        
        <ComboBox x:Name="SortComboBox" 
                  Width="200" Height="30" 
                  Grid.RowSpan="2" 
                  Grid.Column="1"
                  VerticalAlignment="Top">
            <ComboBoxItem Content="Сначала недорогие" IsSelected="True"/>
            <ComboBoxItem Content="Сначала дорогие"/>
            <ComboBoxItem Content="По новинкам"/>
            <ComboBoxItem Content="По скидке"/>
            <ComboBoxItem Content="По количеству отзывов"/>
            <ComboBoxItem Content="Сначала с лучшей оценкой"/>
        </ComboBox>

        <ListView x:Name="ListProductsListView" Width="200" 
                  HorizontalAlignment="Center" 
                  Grid.Row="2"
                  Grid.Column="1">
            
        </ListView>
        
        <ListBox x:Name="DasketListBox" Width="200" 
                 HorizontalAlignment="Right" 
                 Margin="3,50,0,83" 
                 Grid.RowSpan="2"
                 Grid.Column="2"
                 >
            <TextBlock FontWeight="Bold" 
                       Text="Наличие" />
            <CheckBox Content="В наличии" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: сегодня" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: завтра" 
                      IsChecked="True"/>
            <CheckBox Content="Под заказ: позже" 
                      IsChecked="True"/>
            <CheckBox Content="Нет в продаже"/>
            <Button Content="Применить"/>
        </ListBox>
    </Grid>
</Window>
